# The bash_profile.symlink file is symlinked to the ~/.bash_profile file.
#
# The bash_profile.symlink file is responsible for sourcing the .bashrc file.

[[ -e "${HOME}/.bashrc" ]] && source "${HOME}/.bashrc"
